{
  "name": "evenai-assistant-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Modular AI assistant platform for branded, domain-specific business assistants.",
  "type": "module",
  "scripts": {
    "validate:knowledge": "node scripts/validate-knowledge.mjs",
    "test:retrieval": "node --test tests/knowledge/*.test.mjs",
    "test:safety": "node --test tests/safety/*.test.mjs",
    "test:conversation": "node --test tests/conversation/*.test.mjs",
    "test:response": "node --test tests/response/*.test.mjs",
    "test:runtime": "node --test tests/runtime/*.test.mjs",
    "test:apps": "node --test tests/apps/*.test.mjs",
    "test:http": "node --test tests/http/*.test.mjs",
    "test": "npm run validate:knowledge && npm run test:retrieval && npm run test:safety && npm run test:conversation && npm run test:response && npm run test:runtime && npm run test:apps && npm run test:http"
  },
  "engines": {
    "node": ">=20"
  }
}
